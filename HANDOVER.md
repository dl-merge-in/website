# Website 引き継ぎ

> 最終更新: 2026-08-02
>
> このファイルは、DL-Merge-In の公開サイトを更新する人・作業セッション向けの引き継ぎです。アプリ本体の実装状況は private リポジトリの `docs/HANDOVER.md` を参照し、このファイルには公開LP・配布導線・GitHub Pagesに関する内容だけを記録します。

## 1. サイトの目的

- 製品の概要を簡潔に伝え、利用者が **配布ファイル** と **セットアップ方法** に迷わず到達できるようにする。
- 公開対象は Windows 向けデスクトップ版。Chrome が標準対応で、インストール時の選択により Microsoft Edge にも対応する。
- 現行版は `0.0.3`。無料・無制限・フル機能として案内している。
- 本体の開発リポジトリは非公開のまま維持する。公開サイトと配布物は、それぞれ public リポジトリで管理する。

## 2. 公開先と役割

| 対象 | URL / リポジトリ | 役割 |
| --- | --- | --- |
| 正規URL | <https://www.dlmergein.com/> | LP の正式な公開URL |
| 裸ドメイン | <https://dlmergein.com/> | 正規URLへ 301 リダイレクト |
| サイト | <https://github.com/dl-merge-in/website> | この静的サイトの公開リポジトリ |
| 配布 | <https://github.com/dl-merge-in/DL-Merge-In/releases> | `Setup.exe`、SHA-256、リリースノートの公開先 |
| DNS | Cloudflare | DNS管理。GitHub Pages用レコードは DNS only（灰色雲） |

GitHub Pages は `website` リポジトリの `main` ブランチのルートを公開する。`CNAME` の値は `www.dlmergein.com` であり、通常のページ更新では変更・削除しない。

## 3. 現在の公開状態

- LP、セットアップガイド、利用条件・免責・プライバシー、対応サービスの例を公開済み。
- `www.dlmergein.com` は HTTPS を有効化済み。`dlmergein.com` から `www` へ転送される。
- ダウンロード用 GitHub Releases は未公開。LPには「公開配布URLは準備中」と表示し、現状はセットアップガイドへ案内している。
- LPの画面画像は中立なモックとDL-Merge-Inの画面に限定済み。第三者サービスのロゴ・アカウント情報・動画タイトルを残さない。

## 4. 最優先の残タスク

### 4.1 LPに Quickstart を追加

トップ画面の早い位置に、導入から初回保存までを3ステップで示す。既存の「使い方」は利用中の操作説明として残し、Quickstart は導入手順に絞る。

1. `Setup.exe` をダウンロードする。
2. セットアップ後、Chrome または Edge で拡張機能を手動で読み込む。
3. 保存したいページを開き、Detect タブで候補を選んで保存する。

注意: 拡張機能の読み込みは完全自動ではない。この点を Quickstart 内でも明記する。Release が未公開の間、1番目のボタンは実在しないダウンロードURLへリンクしない。

### 4.2 初回リリースを公開し、CTAを接続

`DL-Merge-In` リポジトリで `v0.0.3` Release を作成し、少なくとも次を添付する。

- `Setup.exe`
- `SHA256SUMS.txt`
- 対象OS、バージョン、主な変更点、既知の注意点
- 新しいWindowsアプリで表示されうる SmartScreen 警告と、DRM保護コンテンツは対象外である旨

Release の公開後、LPの主ボタンを該当する Release URL へ変更する。最新版を常に示したい場合でも、Release一覧ではなく特定バージョンのURLを使い、更新時に明示的に差し替える。

### 4.3 公開用セットアップガイドを確認する

`guide/index.html` はインストーラーに同梱されるガイドを基にしている。公開サイト上では拡張機能フォルダの実パスを自動取得できないため、Release公開前に次を確認する。

- Web上で手順の意味が通じるか。
- インストーラー起動後に開くローカルガイドとの役割分担が明確か。
- LPからガイドへ移動した利用者が、配布ファイルへ戻れるか。

必要なら、公開サイト用の事前案内ページと、インストーラー内の実操作ガイドを分ける。30〜60秒程度の無音または字幕付きインストール動画は、実際の手順が固まってから追加を検討する。

## 5. 正確な表現の基準

- 製品名は **DL-Merge-In**。旧表記 `DL-mergin` は使わない。
- 「対応サイト数」や取得可否を保証しない。専用対応サイトに加え、yt-dlp対応サイトと独自の汎用検出でも候補を探す、と限定して表現する。
- 字幕は **YouTubeの字幕を別ファイルで保存できる場合がある** という限定的な表現にする。全サイト・全動画での字幕取得を示唆しない。
- DRM保護コンテンツの回避は対象外。利用者が権利・必要な許可を持つコンテンツのみを扱い、サービス規約と適用法令を守るよう案内する。
- 第三者サービス名は `supported-sites/` の例示にのみ平文で掲載する。ロゴ、サービス固有の配色・UI、提携・推奨を示す表現は使わない。

## 6. 更新時の確認リスト

### LP本文・画像

- [ ] 主CTAが現在の配布状況と一致している。
- [ ] Windows、Chrome、Edge、手動の拡張機能読み込み、DRMの説明に誤りがない。
- [ ] 画面画像に個人情報、アカウント、第三者ロゴ、動画タイトルやサムネイルが含まれない。
- [ ] `legal/` と `supported-sites/` の内容がLP本文と矛盾しない。

### 動作・公開

- [ ] ローカルWebサーバー経由で `/`、`/guide/`、`/legal/`、`/supported-sites/` のリンクと戻り導線を確認する。
- [ ] PC幅とモバイル幅で、ヘッダー、CTA、フッター、FAQを確認する。
- [ ] `git diff --check` を実行する。
- [ ] `main` へpush後、<https://www.dlmergein.com/> で公開内容・HTTPS・正規URLへの転送を確認する。

## 7. 編集範囲

```text
index.html              LP本体
style.css               LP・下層ページのスタイル
script.js               軽い画面効果と画像拡大表示
assets/                 OGP画像と匿名化済み画面画像
guide/index.html        セットアップガイド
legal/index.html        利用条件・免責・プライバシー
supported-sites/index.html  対応サービスの例と注意事項
CNAME                   GitHub Pagesの独自ドメイン設定（原則変更しない）
```

## 8. 問い合わせと公開方針

- 不具合報告・機能要望は `website` リポジトリの Issues を使用する。個人情報、Cookie、アクセストークン、非公開URLを投稿させない。
- 法務・削除要請の連絡先は `dlmergein@proton.me`。
- 外部Pull Requestは受け付けない。サイトのソース、文書、画像は [LICENSE](LICENSE) のとおり転載・改変を許可しない。

## 9. 次回作業の始め方

1. この `HANDOVER.md` と `README.md` を読む。
2. `git status --short --branch` で未コミット変更がないか確認する。
3. まず §4.1 の Quickstart を実装し、公開前チェックを行う。
4. Release を作成した段階で §4.2 のCTA接続を行う。
